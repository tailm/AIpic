# Stable Diffusion Web UI requirements for macOS
# This file includes version constraints to ensure compatibility on macOS

# Core dependencies
torch==1.13.1
torchvision==0.14.1
torchmetrics<1.0.0  # Version 0.11.4 works with torch 1.13.1

# Web UI framework
gradio==3.16.2

# Image processing
Pillow==9.4.0
opencv-python==4.8.1.78  # Version compatible with numpy<2
numpy==1.23.3  # Version compatible with blendmodes and opencv-python
scikit-image>=0.19,<0.20

# AI/ML libraries
transformers==4.25.1
accelerate==0.12.0
einops==0.4.1
timm==0.6.7  # Updated from 0.4.12 for compatibility
pytorch_lightning==1.7.6  # Updated from 1.7.7 for compatibility

# Model and data handling
safetensors==0.2.7
omegaconf==2.2.3
kornia==0.6.7
torchdiffeq==0.2.3
torchsde==0.2.5

# Image enhancement libraries (optional, may have compatibility issues on macOS)
# basicsr==1.3.1  # Version without tb-nightly dependency
# facexlib==0.3.0
# gfpgan  # Skipped due to tb-nightly dependency on macOS
# realesrgan==0.2.9  # Older version without basicsr>=1.4.2 dependency

# File and network operations
requests==2.25.1
GitPython==3.1.30
piexif==1.1.3
jsonmerge==1.8.0
lark==1.1.2
inflection==0.5.1
clean-fid==0.1.29
resize-right==0.0.2

# Web and API
fastapi==0.94.0
httpx==0.24.1  # Compatible with httpcore 0.15.0
httpcore==0.15.0

# System utilities
psutil

# Fonts
fonts
font-roboto

# Other utilities
blendmodes==2022
invisible-watermark

# Note: The following packages have compatibility issues on macOS ARM:
# - basicsr>=1.4.2 requires tb-nightly which is not available on macOS ARM
# - gfpgan requires basicsr>=1.4.2
# - realesrgan>=0.3.0 requires basicsr>=1.4.2
# 
# Workarounds implemented:
# 1. Use basicsr==1.3.1 (older version without tb-nightly dependency)
# 2. Skip gfpgan installation (commented out in webui-user.sh)
# 3. Use realesrgan==0.2.9 (older version compatible with basicsr 1.3.1)
# 4. Added load_file_from_url function to basicsr.utils.download_util
# 5. Modified modules to handle missing gfpgan and realesrgan gracefully

# To install with these constraints, use:
# pip install -r requirements_macos.txt